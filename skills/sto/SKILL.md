---
name: sto
description: Use only when explicitly invoked as sto. Reply with a simple table only, or turn the last answer into a simple table when no arguments are provided.
license: MIT
metadata:
  version: "1.0.0"
---

Reply with only a simple table for what is asked in $ARGUMENTS. If nothing is provided, turn the last answer into a simple table.
