---
name: osw
description: Use only when explicitly invoked as osw. Reply in one line using simple words, or simplify the last answer when no arguments are provided.
license: MIT
metadata:
  version: "1.0.0"
---

Reply in one line, using simple words, to what is asked in $ARGUMENTS. If nothing is provided, rewrite the last answer in one line using simple words.
